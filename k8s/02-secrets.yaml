apiVersion: v1
kind: Secret
metadata:
  name: workflow-secrets
  namespace: workflow-engine
type: Opaque
stringData:
  database-url: "postgres://workflow:workflow_password@postgres:5432/workflow?sslmode=disable"
  jwt-secret: "change-this-in-production"
  s3-access-key: "minioadmin"
  s3-secret-key: "minioadmin"
  smtp-user: "test"
  smtp-pass: "test"
