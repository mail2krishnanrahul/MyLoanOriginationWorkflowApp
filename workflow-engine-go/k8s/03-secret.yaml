apiVersion: v1
kind: Secret
metadata:
  name: workflow-db-secret
  namespace: workflow-engine
type: Opaque
stringData:
  # NOTE: In a real production environment, use a secret manager like AWS Secrets Manager or HashiCorp Vault.
  connection-string: "postgres://myappuser:password@postgres:5432/LoanOriginationDB?sslmode=require"
